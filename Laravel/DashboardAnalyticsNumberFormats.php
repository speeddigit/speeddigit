<?php 

// $formatter = new NumberFormatter('en-US', NumberFormatter::CURRENCY);
// $formatter = new NumberFormatter('en-IND', NumberFormatter::CURRENCY);
// $formatter = new NumberFormatter('bn-BD', NumberFormatter::CURRENCY);

echo NumberFormatter::format(147570);

// echo NumberFormatter::CURRENCY;